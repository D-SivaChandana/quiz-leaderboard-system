# quiz-leaderboard-system
A Java-based backend application that polls a quiz API, removes duplicate event data, aggregates participant scores, and generates an accurate leaderboard using idempotent processing.
