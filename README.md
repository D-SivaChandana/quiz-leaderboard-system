# Quiz Leaderboard System
A Java-based backend application,this project polls an external quiz API 10 times, removes duplicate events using (roundId + participant), aggregates scores, and generates a leaderboard using idempotent processing.
# Features
- API polling (10 rounds)
- Duplicate event handling
- Score aggregation
- Leaderboard generation
# Tech Stack
- Java
